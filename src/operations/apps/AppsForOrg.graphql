query AppsForOrg($orgSlug: String!) {
  appsForOrg(orgSlug: $orgSlug) {
    appId
    orgId
    name
    slug
    description
    visibility
    status
    metadata
    splitMode
    gameApiUrl
    createdAt
    updatedAt
  }
}
